[Unit]
Description=Cinemata VictoriaTraces
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=cinematacms-observability
Group=cinematacms-observability
EnvironmentFile=/etc/cinematacms-observability/stack.env
ExecStart=/opt/cinematacms-observability/bin/victoria-traces-prod -httpListenAddr=127.0.0.1:${VICTORIATRACES_PORT} -storageDataPath=/var/lib/victoriatraces -retentionPeriod=${TRACES_RETENTION}
Restart=on-failure
RestartSec=5
NoNewPrivileges=true
PrivateTmp=true
ProtectHome=true
ProtectSystem=strict
ReadWritePaths=/var/lib/victoriatraces

[Install]
WantedBy=multi-user.target
